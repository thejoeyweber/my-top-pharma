/**
 * Application Constants
 * 
 * Central location for application constants.
 */

/**
 * UI Constants
 */
export const UI = {
  PLACEHOLDER_IMAGE: '/assets/placeholders/default.svg',
  DEFAULT_PAGE_SIZE: 20,
};

/**
 * Application Routes
 */
export const ROUTES = {
  HOME: '/',
  DASHBOARD: '/dashboard',
  LOGIN: '/login',
  REGISTER: '/register',
};

/**
 * API Endpoints
 */
export const API_ENDPOINTS = {
  AUTH: '/api/auth',
}; 