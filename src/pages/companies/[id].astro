---
/**
 * Company Detail Page
 * 
 * Displays detailed information about a specific pharmaceutical company.
 */
import DashboardLayout from '../../components/templates/DashboardLayout.astro';
import Card from '../../components/atoms/Card.astro';
import Button from '../../components/atoms/Button.astro';
import FollowButton from '../../components/molecules/FollowButton.astro';
import TabGroup from '../../components/molecules/TabGroup.astro';
import KeyMetric from '../../components/molecules/KeyMetric.astro';
import Timeline from '../../components/molecules/Timeline.astro';
import FinancialChart from '../../components/molecules/FinancialChart.astro';
import PipelineChart from '../../components/molecules/PipelineChart.astro';
import DataTable from '../../components/molecules/DataTable.astro';

// Import data utilities (supports both static and database sources)
import { 
  getAllCompanies as getCompanies,
  getCompanyById, 
  getCompaniesByTherapeuticArea as getRelatedCompanies 
} from '../../utils/companyData';

// Import other existing utilities
import { 
  therapeuticAreas,
  getProductsByCompany,
  websiteCategories
} from '../../utils/dataUtils';
import { getWebsitesByCompany } from '../../utils/crossReferenceUtils';
import { getCompanyLogoUrl, getCompanyHeaderUrl, getWebsiteScreenshotUrl } from '../../utils/assetUtils';

// Generate static paths for all companies
export async function getStaticPaths() {
  const companies = await getCompanies();
  return companies.map(company => ({
    params: { id: company.id },
  }));
}

// Get company ID from URL parameters
const { id } = Astro.params;
const company = await getCompanyById(id as string);

// Handle case when company is not found
if (!company) {
  return Astro.redirect('/companies');
}

// Get related companies (now using async function)
const relatedCompanies = await getRelatedCompanies(company.id);

// Get company products
const companyProducts = getProductsByCompany(company.id);

// Get company websites
const companyWebsites = getWebsitesByCompany(company.id);

// Function to get category label
const getCategoryLabel = (categoryId: string) => {
  const category = websiteCategories.find(c => c.value === categoryId);
  return category ? category.label : categoryId;
};

// Group websites by category for easier display
const websitesByCategory = companyWebsites.reduce((acc, website) => {
  const category = website.category || 'other';
  if (!acc[category]) {
    acc[category] = [];
  }
  acc[category].push(website);
  return acc;
}, {} as Record<string, any[]>);

// Default tab is "overview"
const activeTab = "overview";

// Define tabs
const tabs = [
  { id: "overview", label: "Overview", icon: '<svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5" fill="none" viewBox="0 0 24 24" stroke="currentColor"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 16h-1v-4h-1m1-4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z" /></svg>' },
  { id: "products", label: "Products", icon: '<svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5" fill="none" viewBox="0 0 24 24" stroke="currentColor"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19.428 15.428a2 2 0 00-1.022-.547l-2.387-.477a6 6 0 00-3.86.517l-.318.158a6 6 0 01-3.86.517L6.05 15.21a2 2 0 00-1.806.547M8 4h8l-1 1v5.172a2 2 0 00.586 1.414l5 5c1.26 1.26.367 3.414-1.415 3.414H4.828c-1.782 0-2.674-2.154-1.414-3.414l5-5A2 2 0 009 10.172V5L8 4z" /></svg>' },
  { id: "websites", label: "Websites", icon: '<svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5" fill="none" viewBox="0 0 24 24" stroke="currentColor"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M21 12a9 9 0 01-9 9m9-9a9 9 0 00-9-9m9 9H3m9 9a9 9 0 01-9-9m9 9c1.657 0 3-4.03 3-9s-1.343-9-3-9m0 18c-1.657 0-3-4.03-3-9s1.343-9 3-9m-9 9a9 9 0 019-9" /></svg>' },
  { id: "pipeline", label: "Pipeline", icon: '<svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5" fill="none" viewBox="0 0 24 24" stroke="currentColor"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 19v-6a2 2 0 00-2-2H5a2 2 0 00-2 2v6a2 2 0 002 2h2a2 2 0 002-2zm0 0V9a2 2 0 012-2h2a2 2 0 012 2v10m-6 0a2 2 0 002 2h2a2 2 0 002-2m0 0V5a2 2 0 012-2h2a2 2 0 012 2v14a2 2 0 01-2 2h-2a2 2 0 01-2-2z" /></svg>' },
  { id: "financials", label: "Financials", icon: '<svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5" fill="none" viewBox="0 0 24 24" stroke="currentColor"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8c-1.657 0-3 .895-3 2s1.343 2 3 2 3 .895 3 2-1.343 2-3 2m0-8c1.11 0 2.08.402 2.599 1M12 8V7m0 1v8m0 0v1m0-1c-1.11 0-2.08-.402-2.599-1M21 12a9 9 0 11-18 0 9 9 0 0118 0z" /></svg>' },
  { id: "stock", label: "Stock", icon: '<svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5" fill="none" viewBox="0 0 24 24" stroke="currentColor"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M7 12l3-3 3 3 4-4M8 21l4-4 4 4M3 4h18M4 4h16v12a1 1 0 01-1 1H5a1 1 0 01-1-1V4z" /></svg>' },
  { id: "news", label: "News", icon: '<svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5" fill="none" viewBox="0 0 24 24" stroke="currentColor"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 20H5a2 2 0 01-2-2V6a2 2 0 012-2h10a2 2 0 012 2v1m2 13a2 2 0 01-2-2V7m2 13a2 2 0 002-2V9a2 2 0 00-2-2h-2m-4-3H9M7 16h6M7 8h6v4H7V8z" /></svg>' },
];

// Get therapeutic area names
const getTherapeuticAreaNames = (areaIds: string[]) => {
  return areaIds.map(id => {
    const area = therapeuticAreas.find(a => a.id === id);
    return area ? area.name : id;
  });
};

const companyTherapeuticAreas = getTherapeuticAreaNames(company.therapeuticAreas);

// Format market cap for display
const formatMarketCap = (marketCap: number) => {
  if (marketCap >= 1) {
    return `$${marketCap.toFixed(1)}B`;
  } else {
    return `$${(marketCap * 1000).toFixed(0)}M`;
  }
};

// Breadcrumbs for company detail page
const breadcrumbs = [
  { label: 'Home', href: '/' },
  { label: 'Companies', href: '/companies' },
  { label: company.name, href: `/companies/${company.id}`, isActive: true },
];

// Prepare financial data
const financialData = {
  revenue: company.financials?.map(f => ({ year: f.year, value: f.revenue })) || [],
  rAndDSpending: company.financials?.map(f => ({ year: f.year, value: f.rAndDSpending })) || [],
  netIncome: company.financials?.map(f => ({ year: f.year, value: f.netIncome })) || []
};

// Prepare pipeline data
const pipelineStages = ['preclinical', 'phase1', 'phase2', 'phase3', 'submitted', 'approved', 'marketed'];
const pipelineData = pipelineStages.map(stage => ({
  stage,
  count: companyProducts.filter(p => p.stage === stage).length
}));

// Prepare product table columns
const productColumns = [
  { 
    key: 'name', 
    label: 'Name', 
    sortable: true,
    render: (value: string, row: any) => `<a href="/products/${row.id}" class="text-[color:var(--color-primary-600)] hover:text-[color:var(--color-primary-900)] hover:underline">${value}</a>`
  },
  { key: 'stage', label: 'Stage', sortable: true },
  { key: 'therapeuticAreas', label: 'Therapeutic Areas', sortable: true, render: (value: string[]) => value.join(', ') },
  { key: 'approvalDate', label: 'Approval Date', sortable: true, render: (value: string) => 
    value ? new Date(value).toLocaleDateString() : '-'
  }
];

// Prepare financial table columns
const financialColumns = [
  { key: 'year', label: 'Year', sortable: true },
  { key: 'revenue', label: 'Revenue (B)', sortable: true, align: 'right', render: (value: number) => `$${value.toFixed(1)}` },
  { key: 'rAndDSpending', label: 'R&D (B)', sortable: true, align: 'right', render: (value: number) => `$${value.toFixed(1)}` },
  { key: 'netIncome', label: 'Net Income (B)', sortable: true, align: 'right', render: (value: number) => `$${value.toFixed(1)}` },
  { 
    key: 'rAndDPercent', 
    label: 'R&D % of Revenue', 
    sortable: true, 
    align: 'right',
    render: (_, row: any) => `${((row.rAndDSpending / row.revenue) * 100).toFixed(1)}%`
  }
];
---

<DashboardLayout
  title={`${company.name} | Top Pharma`}
  description={`Company profile for ${company.name} - View detailed information, products, financials, and news.`}
  currentPath={`/companies/${company.id}`}
  breadcrumbs={breadcrumbs}
>
  <div class="space-y-6">
    <!-- Company Header with cover image, logo, name, headline -->
    <div class="bg-white rounded-lg border border-gray-200 shadow-sm overflow-hidden">
      <div class="h-48 bg-gradient-to-r from-[color:var(--color-primary-600)] to-[color:var(--color-primary-400)] relative">
        {company.headerImageUrl && (
          <img
            src={company.headerImageUrl}
            alt={`${company.name} header`}
            class="w-full h-full object-cover absolute inset-0 mix-blend-overlay"
          />
        )}
        <div class="absolute inset-0 bg-black/20"></div>
      </div>
      
      <div class="px-6 sm:px-8 relative">
        <!-- Logo -->
        <div class="flex flex-col sm:flex-row">
          <div class="-mt-12 mb-6 sm:mb-0 flex items-start">
            <div class="w-24 h-24 bg-white rounded-lg border-4 border-white shadow-md flex items-center justify-center relative">
              {company.logoUrl ? (
                <img src={company.logoUrl} alt={`${company.name} logo`} class="w-16 h-16 object-contain" />
              ) : (
                <span class="text-3xl font-bold text-[color:var(--color-primary-600)]">{company.name.charAt(0)}</span>
              )}
            </div>
          </div>
          
          <div class="sm:ml-6 flex-1">
            <div class="flex flex-col sm:flex-row sm:items-center sm:justify-between mb-4">
              <div>
                <h1 class="text-2xl font-bold text-gray-900">{company.name}</h1>
                <div class="flex items-center text-gray-500 mt-1">
                  <svg xmlns="http://www.w3.org/2000/svg" class="h-4 w-4 mr-1" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17.657 16.657L13.414 20.9a1.998 1.998 0 01-2.827 0l-4.244-4.243a8 8 0 1111.314 0z" />
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 11a3 3 0 11-6 0 3 3 0 016 0z" />
                  </svg>
                  <span>{company.headquarters}</span>
                
                  {company.stockSymbol && (
                    <>
                      <span class="mx-2">•</span>
                      <svg xmlns="http://www.w3.org/2000/svg" class="h-4 w-4 mr-1" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 19v-6a2 2 0 00-2-2H5a2 2 0 00-2 2v6a2 2 0 002 2h2a2 2 0 002-2zm0 0V9a2 2 0 012-2h2a2 2 0 012 2v10m-6 0a2 2 0 002 2h2a2 2 0 002-2m0 0V5a2 2 0 012-2h2a2 2 0 012 2v14a2 2 0 01-2 2h-2a2 2 0 01-2-2z" />
                      </svg>
                      <span>{company.stockSymbol} ({company.stockExchange})</span>
                    </>
                  )}
                  
                  <span class="mx-2">•</span>
                  <svg xmlns="http://www.w3.org/2000/svg" class="h-4 w-4 mr-1" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M21 12a9 9 0 01-9 9m9-9a9 9 0 00-9-9m9 9H3m9 9a9 9 0 01-9-9m9 9c1.657 0 3-4.03 3-9s-1.343-9-3-9m0 18c-1.657 0-3-4.03-3-9s1.343-9 3-9m0 18c1.657 0 3-4.03 3-9s-1.343-9-3-9" />
                  </svg>
                  <a href={company.website} target="_blank" rel="noopener noreferrer" class="text-[color:var(--color-primary-600)] hover:underline">
                    Website
                  </a>
                </div>
              </div>
              
              <div class="flex gap-2 mt-4 sm:mt-0">
                <FollowButton company={company} />
                <Button variant="outline" size="sm">
                  <svg xmlns="http://www.w3.org/2000/svg" class="-ml-0.5 mr-1.5 h-4 w-4" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 8l7.89 5.26a2 2 0 002.22 0L21 8M5 19h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v10a2 2 0 002 2z" />
                  </svg>
                  Share
                </Button>
              </div>
            </div>
            
            <!-- Key Metrics -->
            <div class="grid grid-cols-2 md:grid-cols-4 gap-3 mb-6">
              <KeyMetric 
                label="Market Cap" 
                value={formatMarketCap(company.marketCap)}
                size="sm"
              />
              <KeyMetric 
                label="Employees" 
                value={company.employees.toLocaleString()}
                size="sm"
              />
              <KeyMetric 
                label="Products" 
                value={company.products.length}
                size="sm"
              />
              <KeyMetric 
                label="Founded" 
                value={company.founded}
                size="sm"
              />
            </div>
          </div>
        </div>
        
        <!-- Tab Navigation -->
        <TabGroup tabs={tabs} activeTab={activeTab} ariaLabel="Company information" />
      </div>
    </div>
    
    <!-- Tab Content -->
    <div>
      <!-- Overview Tab -->
      <div id="tab-content-overview" role="tabpanel" aria-labelledby="tab-overview">
        <div class="grid grid-cols-1 lg:grid-cols-3 gap-6">
          <!-- Main Column -->
          <div class="lg:col-span-2 space-y-6">
            <!-- Company Description -->
            <Card>
              <div class="p-6">
                <h2 class="text-lg font-medium text-gray-900 mb-4">About {company.name}</h2>
                <div class="prose max-w-none">
                  <p>{company.description}</p>
                </div>
              </div>
            </Card>
            
            <!-- Key Milestones -->
            <Card>
              <div class="p-6">
                <h2 class="text-lg font-medium text-gray-900 mb-4">Key Milestones</h2>
                <div class="relative">
                  {/* Timeline line */}
                  <div class="absolute top-0 left-4 h-full w-0.5 bg-gray-200"></div>
                  
                  <div class="space-y-6">
                    {company.milestones.sort((a, b) => new Date(b.date).getTime() - new Date(a.date).getTime()).map(milestone => (
                      <div class="relative pl-10">
                        <div class="absolute left-0 top-1.5 h-8 w-8 rounded-full border-2 border-[color:var(--color-primary-500)] bg-white flex items-center justify-center">
                          {milestone.type === 'acquisition' && (
                            <svg xmlns="http://www.w3.org/2000/svg" class="h-4 w-4 text-[color:var(--color-primary-500)]" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 9V7a2 2 0 00-2-2H5a2 2 0 00-2 2v6a2 2 0 002 2h2m2 4h10a2 2 0 002-2v-6a2 2 0 00-2-2H9a2 2 0 00-2 2v6a2 2 0 002 2zm7-5a2 2 0 11-4 0 2 2 0 014 0z" />
                            </svg>
                          )}
                          {milestone.type === 'regulatory' && (
                            <svg xmlns="http://www.w3.org/2000/svg" class="h-4 w-4 text-[color:var(--color-primary-500)]" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z" />
                            </svg>
                          )}
                          {milestone.type === 'corporate' && (
                            <svg xmlns="http://www.w3.org/2000/svg" class="h-4 w-4 text-[color:var(--color-primary-500)]" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 21V5a2 2 0 00-2-2H7a2 2 0 00-2 2v16m14 0h2m-2 0h-5m-9 0H3m2 0h5M9 7h1m-1 4h1m4-4h1m-1 4h1m-5 10v-5a1 1 0 011-1h2a1 1 0 011 1v5m-4 0h4" />
                            </svg>
                          )}
                          {milestone.type === 'product' && (
                            <svg xmlns="http://www.w3.org/2000/svg" class="h-4 w-4 text-[color:var(--color-primary-500)]" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19.428 15.428a2 2 0 00-1.022-.547l-2.387-.477a6 6 0 00-3.86.517l-.318.158a6 6 0 01-3.86.517L6.05 15.21a2 2 0 00-1.806.547M8 4h8l-1 1v5.172a2 2 0 00.586 1.414l5 5c1.26 1.26.367 3.414-1.415 3.414H4.828c-1.782 0-2.674-2.154-1.414-3.414l5-5A2 2 0 009 10.172V5L8 4z" />
                            </svg>
                          )}
                        </div>
                        <div>
                          <div class="flex items-center text-sm">
                            <span class="font-medium text-gray-900">{milestone.title}</span>
                            <span class="ml-2 text-gray-500">
                              {new Date(milestone.date).toLocaleDateString(undefined, { year: 'numeric', month: 'short', day: 'numeric' })}
                            </span>
                          </div>
                          <p class="mt-1 text-sm text-gray-600">{milestone.description}</p>
                        </div>
                      </div>
                    ))}
                  </div>
                </div>
              </div>
            </Card>
          </div>
          
          <!-- Sidebar -->
          <div class="space-y-6">
            <!-- Therapeutic Areas -->
            <Card>
              <div class="p-6">
                <h2 class="text-lg font-medium text-gray-900 mb-4">Therapeutic Areas</h2>
                <div class="space-y-2">
                  {companyTherapeuticAreas.map(area => (
                    <div class="flex items-center">
                      <div class="w-2 h-2 rounded-full bg-[color:var(--color-primary-500)] mr-2"></div>
                      <span>{area}</span>
                    </div>
                  ))}
                </div>
              </div>
            </Card>
            
            <!-- Related Companies -->
            <Card>
              <div class="p-6">
                <h2 class="text-lg font-medium text-gray-900 mb-4">Related Companies</h2>
                <div class="space-y-4">
                  {relatedCompanies.map(related => (
                    related.company && (
                      <div class="flex items-center">
                        <div class="w-8 h-8 bg-gray-100 rounded-full flex items-center justify-center mr-3">
                          {related.company.logoUrl ? (
                            <img src={related.company.logoUrl} alt={`${related.company.name} logo`} class="w-6 h-6 object-contain" />
                          ) : (
                            <span class="text-xs font-bold text-primary-600">{related.company.name.charAt(0)}</span>
                          )}
                        </div>
                        <div class="flex-1">
                          <a href={`/companies/${related.company.id}`} class="text-sm font-medium text-gray-900 hover:text-primary-600">
                            {related.company.name}
                          </a>
                          <p class="text-xs text-gray-500 capitalize">{related.relationship}</p>
                        </div>
                      </div>
                    )
                  ))}
                </div>
              </div>
            </Card>
          </div>
        </div>
      </div>
      
      <!-- Products Tab -->
      <div id="tab-content-products" role="tabpanel" aria-labelledby="tab-products" class="hidden">
        <div class="grid gap-6">
          <Card>
            <div class="p-6">
              <div class="flex justify-between items-center mb-6">
                <h2 class="text-lg font-medium text-gray-900">Products & Medicines</h2>
                <div class="flex gap-2">
                  <div class="relative">
                    <input
                      type="text"
                      placeholder="Search products..."
                      class="rounded-md border border-gray-300 py-2 pl-10 pr-4 text-sm placeholder-gray-500 focus:border-primary-500 focus:outline-none focus:ring-1 focus:ring-primary-500"
                    />
                    <div class="pointer-events-none absolute inset-y-0 left-0 flex items-center pl-3">
                      <svg xmlns="http://www.w3.org/2000/svg" class="h-4 w-4 text-gray-400" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0z" />
                      </svg>
                    </div>
                  </div>
                  <select
                    class="rounded-md border border-gray-300 py-2 px-3 text-sm focus:border-primary-500 focus:outline-none focus:ring-1 focus:ring-primary-500"
                  >
                    <option value="">All Statuses</option>
                    <option value="approved">Approved</option>
                    <option value="phase3">Phase 3</option>
                    <option value="phase2">Phase 2</option>
                    <option value="phase1">Phase 1</option>
                  </select>
                </div>
              </div>
              
              <!-- Products Table -->
              <div class="overflow-x-auto">
                <table class="min-w-full divide-y divide-gray-200">
                  <thead class="bg-gray-50">
                    <tr>
                      <th scope="col" class="py-3.5 pl-4 pr-3 text-left text-sm font-semibold text-gray-900">Name</th>
                      <th scope="col" class="px-3 py-3.5 text-left text-sm font-semibold text-gray-900">Description</th>
                      <th scope="col" class="px-3 py-3.5 text-left text-sm font-semibold text-gray-900">Status</th>
                      <th scope="col" class="px-3 py-3.5 text-left text-sm font-semibold text-gray-900">Therapeutic Areas</th>
                      <th scope="col" class="px-3 py-3.5 text-left text-sm font-semibold text-gray-900">Approval Date</th>
                    </tr>
                  </thead>
                  <tbody class="divide-y divide-gray-200 bg-white">
                    {company.products.map(product => (
                      <tr>
                        <td class="whitespace-nowrap py-4 pl-4 pr-3 text-sm font-medium text-gray-900">
                          <a href={`/products/${product.id}`} class="text-[color:var(--color-primary-600)] hover:text-[color:var(--color-primary-900)] hover:underline">
                            {product.name}
                          </a>
                        </td>
                        <td class="px-3 py-4 text-sm text-gray-500">{product.description}</td>
                        <td class="px-3 py-4 text-sm">
                          <span class:list={[
                            "inline-flex items-center rounded-full px-2.5 py-0.5 text-xs font-medium",
                            product.status === "Approved" 
                              ? "bg-green-100 text-green-800" 
                              : product.status === "Phase 3"
                                ? "bg-blue-100 text-blue-800"
                                : product.status === "Phase 2"
                                  ? "bg-purple-100 text-purple-800"
                                  : "bg-yellow-100 text-yellow-800"
                          ]}>
                            {product.status}
                          </span>
                        </td>
                        <td class="px-3 py-4 text-sm text-gray-500">
                          <div class="flex flex-wrap gap-1">
                            {getTherapeuticAreaNames(product.therapeuticAreas).map(area => (
                              <span class="inline-flex items-center rounded-full bg-[color:var(--color-primary-50)] px-2 py-0.5 text-xs font-medium text-[color:var(--color-primary-700)]">
                                {area}
                              </span>
                            ))}
                          </div>
                        </td>
                        <td class="px-3 py-4 text-sm text-gray-500">
                          {product.approvalDate 
                            ? new Date(product.approvalDate).toLocaleDateString(undefined, { year: 'numeric', month: 'short', day: 'numeric' })
                            : '-'
                          }
                        </td>
                      </tr>
                    ))}
                  </tbody>
                </table>
              </div>
            </div>
          </Card>
        </div>
      </div>
      
      <!-- Websites Tab Content -->
      <div id="tab-content-websites" role="tabpanel" aria-labelledby="tab-websites" class="space-y-6 hidden">
        <Card>
          <div class="p-6">
            <h2 class="text-xl font-medium text-gray-900 mb-4">Company Websites</h2>
            
            {companyWebsites.length === 0 ? (
              <p class="text-gray-500">No websites found for this company.</p>
            ) : (
              <div class="space-y-6">
                {Object.entries(websitesByCategory).map(([category, websites]) => (
                  <div class="space-y-3">
                    <h3 class="text-lg font-medium text-gray-900">{getCategoryLabel(category)}</h3>
                    <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-4">
                      {websites.map(website => (
                        <div class="bg-white rounded-lg border border-gray-200 shadow-sm overflow-hidden">
                          <div class="p-4">
                            <div class="flex justify-between items-start">
                              <h4 class="font-medium text-gray-900 mb-1">{website.domain}</h4>
                              <span class="inline-flex items-center rounded-full bg-blue-50 px-2 py-1 text-xs font-medium text-blue-700">
                                {getCategoryLabel(website.category)}
                              </span>
                            </div>
                            
                            {website.siteName && (
                              <p class="text-sm text-gray-500 mb-2">{website.siteName}</p>
                            )}
                            
                            <p class="text-sm text-gray-700 line-clamp-2 mb-3">
                              {website.description || 'No description available.'}
                            </p>
                            
                            <div class="flex justify-between items-center">
                              <a 
                                href={`/websites/${website.domain.replace(/[^a-zA-Z0-9-]/g, '-').toLowerCase()}`} 
                                class="text-sm text-blue-600 hover:text-blue-800 font-medium"
                              >
                                View details
                              </a>
                              
                              <a 
                                href={`https://${website.domain}`} 
                                target="_blank" 
                                rel="noopener noreferrer" 
                                class="inline-flex items-center text-sm text-gray-500 hover:text-gray-700"
                              >
                                <svg xmlns="http://www.w3.org/2000/svg" class="h-4 w-4 mr-1" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 6H6a2 2 0 00-2 2v10a2 2 0 002 2h10a2 2 0 002-2v-4M14 4h6m0 0v6m0-6L10 14" />
                                </svg>
                                Visit site
                              </a>
                            </div>
                          </div>
                        </div>
                      ))}
                    </div>
                  </div>
                ))}
              </div>
            )}
          </div>
        </Card>
        
        <Card>
          <div class="p-6">
            <h2 class="text-xl font-medium text-gray-900 mb-4">Website Stats</h2>
            
            <div class="grid grid-cols-1 md:grid-cols-3 gap-6 mb-6">
              <KeyMetric
                label="Total Websites"
                value={companyWebsites.length.toString()}
                icon='<svg xmlns="http://www.w3.org/2000/svg" class="h-8 w-8 text-blue-500" fill="none" viewBox="0 0 24 24" stroke="currentColor"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M21 12a9 9 0 01-9 9m9-9a9 9 0 00-9-9m9 9H3m9 9a9 9 0 01-9-9m9 9c1.657 0 3-4.03 3-9s-1.343-9-3-9m0 18c-1.657 0-3-4.03-3-9s1.343-9 3-9m-9 9a9 9 0 019-9" /></svg>'
              />
              
              <KeyMetric
                label="HCP Websites"
                value={(companyWebsites.filter(w => w.category === 'hcp').length).toString()}
                icon='<svg xmlns="http://www.w3.org/2000/svg" class="h-8 w-8 text-blue-500" fill="none" viewBox="0 0 24 24" stroke="currentColor"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M16 7a4 4 0 11-8 0 4 4 0 018 0zM12 14a7 7 0 00-7 7h14a7 7 0 00-7-7z" /></svg>'
              />
              
              <KeyMetric
                label="Patient Websites"
                value={(companyWebsites.filter(w => w.category === 'patient').length).toString()}
                icon='<svg xmlns="http://www.w3.org/2000/svg" class="h-8 w-8 text-blue-500" fill="none" viewBox="0 0 24 24" stroke="currentColor"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 20h5v-2a3 3 0 00-5.356-1.857M17 20H7m10 0v-2c0-.656-.126-1.283-.356-1.857M7 20H2v-2a3 3 0 015.356-1.857M7 20v-2c0-.656.126-1.283.356-1.857m0 0a5.002 5.002 0 019.288 0M15 7a3 3 0 11-6 0 3 3 0 016 0zm6 3a2 2 0 11-4 0 2 2 0 014 0zM7 10a2 2 0 11-4 0 2 2 0 014 0z" /></svg>'
              />
            </div>
            
            {companyWebsites.length > 0 && (
              <div>
                <h3 class="text-sm font-medium text-gray-900 mb-2">Website Categories</h3>
                <div class="flex flex-wrap gap-2">
                  {Object.entries(websitesByCategory).map(([category, websites]) => (
                    <div class="inline-flex items-center rounded-full bg-gray-100 px-3 py-1 text-sm font-medium text-gray-800">
                      {getCategoryLabel(category)}
                      <span class="ml-1 text-gray-500">{websites.length}</span>
                    </div>
                  ))}
                </div>
              </div>
            )}
          </div>
        </Card>
      </div>
      
      <!-- Pipeline Tab (Placeholder) -->
      <div id="tab-content-pipeline" role="tabpanel" aria-labelledby="tab-pipeline" class="hidden">
        <div class="grid grid-cols-1 gap-6">
          <!-- Pipeline Overview -->
          <Card>
            <div class="p-6">
              <h2 class="text-lg font-medium text-[color:var(--color-gray-900)] mb-4">Pipeline Overview</h2>
              <PipelineChart
                id="company-pipeline-chart"
                title="Products by Development Stage"
                data={pipelineData}
              />
            </div>
          </Card>

          <!-- Upcoming Milestones -->
          <Card>
            <div class="p-6">
              <h2 class="text-lg font-medium text-[color:var(--color-gray-900)] mb-4">
                Upcoming Pipeline Milestones
              </h2>
              
              <!-- Timeline visualization -->
              <div class="relative pb-12">
                <div class="absolute left-8 top-0 h-full w-0.5 bg-[color:var(--color-gray-200)]"></div>
                
                {/* Milestone 1 */}
                <div class="relative flex items-start mb-8">
                  <div class="absolute left-0 mt-1 flex h-16 w-16 flex-none items-center justify-center rounded-full bg-[color:var(--color-blue-50)] ring-8 ring-white">
                    <span class="text-xs font-semibold text-[color:var(--color-blue-700)]">Q2 2023</span>
                  </div>
                  <div class="ml-24">
                    <div class="flex items-center space-x-2">
                      <span class="inline-flex items-center rounded-full bg-[color:var(--color-green-100)] px-2.5 py-0.5 text-xs font-medium text-[color:var(--color-green-800)]">
                        Phase 3 Results
                      </span>
                      <h3 class="text-base font-semibold text-[color:var(--color-gray-900)]">DRG-297 for Rheumatoid Arthritis</h3>
                    </div>
                    <p class="mt-1 text-sm text-[color:var(--color-gray-600)]">
                      Phase 3 trial results expected in May 2023. Primary endpoint is ACR20 response at week 24.
                    </p>
                    <div class="mt-2 flex items-center">
                      <div class="flex items-center">
                        <span class="text-xs text-[color:var(--color-gray-500)]">Probability of Success:</span>
                        <span class="ml-1 text-xs font-medium text-[color:var(--color-green-600)]">72%</span>
                      </div>
                      <span class="mx-2 text-[color:var(--color-gray-300)]">•</span>
                      <div class="flex items-center">
                        <span class="text-xs text-[color:var(--color-gray-500)]">Estimated Market:</span>
                        <span class="ml-1 text-xs font-medium">$3.8B</span>
                      </div>
                    </div>
                  </div>
                </div>
                
                {/* Milestone 2 */}
                <div class="relative flex items-start mb-8">
                  <div class="absolute left-0 mt-1 flex h-16 w-16 flex-none items-center justify-center rounded-full bg-[color:var(--color-purple-50)] ring-8 ring-white">
                    <span class="text-xs font-semibold text-[color:var(--color-purple-700)]">Q3 2023</span>
                  </div>
                  <div class="ml-24">
                    <div class="flex items-center space-x-2">
                      <span class="inline-flex items-center rounded-full bg-[color:var(--color-yellow-100)] px-2.5 py-0.5 text-xs font-medium text-[color:var(--color-yellow-800)]">
                        FDA Submission
                      </span>
                      <h3 class="text-base font-semibold text-[color:var(--color-gray-900)]">LPT-089 for Type 2 Diabetes</h3>
                    </div>
                    <p class="mt-1 text-sm text-[color:var(--color-gray-600)]">
                      New Drug Application submission planned for August 2023 following successful Phase 3 results.
                    </p>
                    <div class="mt-2 flex items-center">
                      <div class="flex items-center">
                        <span class="text-xs text-[color:var(--color-gray-500)]">Probability of Approval:</span>
                        <span class="ml-1 text-xs font-medium text-[color:var(--color-green-600)]">85%</span>
                      </div>
                      <span class="mx-2 text-[color:var(--color-gray-300)]">•</span>
                      <div class="flex items-center">
                        <span class="text-xs text-[color:var(--color-gray-500)]">Estimated Market:</span>
                        <span class="ml-1 text-xs font-medium">$2.5B</span>
                      </div>
                    </div>
                  </div>
                </div>
                
                {/* Milestone 3 */}
                <div class="relative flex items-start">
                  <div class="absolute left-0 mt-1 flex h-16 w-16 flex-none items-center justify-center rounded-full bg-[color:var(--color-red-50)] ring-8 ring-white">
                    <span class="text-xs font-semibold text-[color:var(--color-red-700)]">Q1 2024</span>
                  </div>
                  <div class="ml-24">
                    <div class="flex items-center space-x-2">
                      <span class="inline-flex items-center rounded-full bg-[color:var(--color-blue-100)] px-2.5 py-0.5 text-xs font-medium text-[color:var(--color-blue-800)]">
                        Phase 2 Results
                      </span>
                      <h3 class="text-base font-semibold text-[color:var(--color-gray-900)]">ONX-552 for Advanced Melanoma</h3>
                    </div>
                    <p class="mt-1 text-sm text-[color:var(--color-gray-600)]">
                      Phase 2 results expected in Q1 2024. Primary endpoint is objective response rate.
                    </p>
                    <div class="mt-2 flex items-center">
                      <div class="flex items-center">
                        <span class="text-xs text-[color:var(--color-gray-500)]">Probability of Success:</span>
                        <span class="ml-1 text-xs font-medium text-[color:var(--color-yellow-600)]">58%</span>
                      </div>
                      <span class="mx-2 text-[color:var(--color-gray-300)]">•</span>
                      <div class="flex items-center">
                        <span class="text-xs text-[color:var(--color-gray-500)]">Estimated Market:</span>
                        <span class="ml-1 text-xs font-medium">$1.2B</span>
                      </div>
                    </div>
                  </div>
                </div>
              </div>
            </div>
          </Card>
          
          <!-- Pipeline Success Rate -->
          <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
            <Card>
              <div class="p-6">
                <h2 class="text-lg font-medium text-[color:var(--color-gray-900)] mb-4">
                  Pipeline Success Rate by Phase
                </h2>
                <div class="h-64 bg-[color:var(--color-gray-50)] rounded-lg border border-[color:var(--color-gray-200)] p-4 flex flex-col justify-between">
                  <div class="flex items-end h-48 space-x-6">
                    <div class="flex flex-col items-center">
                      <div class="w-16 bg-[color:var(--color-blue-200)] rounded-t-md relative" style="height: 90%">
                        <div class="absolute bottom-0 w-full h-3/5 bg-[color:var(--color-blue-500)] rounded-t-sm"></div>
                        <div class="absolute -top-6 w-full text-center text-sm font-medium text-[color:var(--color-gray-900)]">92%</div>
                      </div>
                      <div class="mt-2 text-xs text-[color:var(--color-gray-600)]">Phase 1</div>
                    </div>
                    <div class="flex flex-col items-center">
                      <div class="w-16 bg-[color:var(--color-blue-200)] rounded-t-md relative" style="height: 65%">
                        <div class="absolute bottom-0 w-full h-3/4 bg-[color:var(--color-blue-500)] rounded-t-sm"></div>
                        <div class="absolute -top-6 w-full text-center text-sm font-medium text-[color:var(--color-gray-900)]">65%</div>
                      </div>
                      <div class="mt-2 text-xs text-[color:var(--color-gray-600)]">Phase 2</div>
                    </div>
                    <div class="flex flex-col items-center">
                      <div class="w-16 bg-[color:var(--color-blue-200)] rounded-t-md relative" style="height: 60%">
                        <div class="absolute bottom-0 w-full h-4/5 bg-[color:var(--color-blue-500)] rounded-t-sm"></div>
                        <div class="absolute -top-6 w-full text-center text-sm font-medium text-[color:var(--color-gray-900)]">60%</div>
                      </div>
                      <div class="mt-2 text-xs text-[color:var(--color-gray-600)]">Phase 3</div>
                    </div>
                    <div class="flex flex-col items-center">
                      <div class="w-16 bg-[color:var(--color-blue-200)] rounded-t-md relative" style="height: 85%">
                        <div class="absolute bottom-0 w-full h-11/12 bg-[color:var(--color-blue-500)] rounded-t-sm"></div>
                        <div class="absolute -top-6 w-full text-center text-sm font-medium text-[color:var(--color-gray-900)]">85%</div>
                      </div>
                      <div class="mt-2 text-xs text-[color:var(--color-gray-600)]">Approval</div>
                    </div>
                  </div>
                  <div class="flex justify-between">
                    <div class="flex items-center">
                      <div class="w-3 h-3 bg-[color:var(--color-blue-500)] rounded-sm mr-1"></div>
                      <span class="text-xs text-[color:var(--color-gray-600)]">{company.name}</span>
                    </div>
                    <div class="flex items-center">
                      <div class="w-3 h-3 bg-[color:var(--color-blue-200)] rounded-sm mr-1"></div>
                      <span class="text-xs text-[color:var(--color-gray-600)]">Industry Avg.</span>
                    </div>
                  </div>
                </div>
              </div>
            </Card>
            
            <!-- Therapeutic Area Focus -->
            <Card>
              <div class="p-6">
                <h2 class="text-lg font-medium text-[color:var(--color-gray-900)] mb-4">
                  Pipeline by Therapeutic Area
                </h2>
                <div class="h-64 bg-[color:var(--color-gray-50)] rounded-lg border border-[color:var(--color-gray-200)] p-4">
                  <!-- Donut chart placeholder -->
                  <div class="flex items-center justify-center h-full">
                    <div class="flex flex-col items-center justify-center">
                      <div class="w-32 h-32 rounded-full border-8 border-[color:var(--color-primary-500)] relative">
                        <div class="absolute inset-0 border-8 border-[color:var(--color-blue-500)]" style="clip-path: polygon(50% 0%, 100% 0%, 100% 100%, 50% 100%)"></div>
                        <div class="absolute inset-0 border-8 border-[color:var(--color-green-500)]" style="clip-path: polygon(50% 0%, 0% 0%, 0% 50%, 50% 50%)"></div>
                        <div class="absolute inset-0 border-8 border-[color:var(--color-amber-500)]" style="clip-path: polygon(0% 50%, 0% 100%, 50% 100%, 50% 50%)"></div>
                      </div>
                    </div>
                  </div>
                  <div class="mt-4 grid grid-cols-2 gap-2">
                    {companyTherapeuticAreas.slice(0, 4).map((area, index) => (
                      <div class="flex items-center">
                        <div class="w-3 h-3 rounded-sm mr-1" style={`background-color: var(--color-${['primary', 'blue', 'green', 'amber'][index]}-500)`}></div>
                        <span class="text-xs text-[color:var(--color-gray-600)]">{area}</span>
                      </div>
                    ))}
                  </div>
                </div>
              </div>
            </Card>
          </div>

          <!-- Products Table -->
          <Card>
            <div class="p-6">
              <h2 class="text-lg font-medium text-[color:var(--color-gray-900)] mb-4">Pipeline Products</h2>
              <DataTable
                id="pipeline-table"
                columns={productColumns}
                data={companyProducts}
                pageSize={10}
                exportable={true}
              />
            </div>
          </Card>
        </div>
      </div>
      
      <!-- Financials Tab -->
      <div id="tab-content-financials" role="tabpanel" aria-labelledby="tab-financials" class="hidden">
        <Card>
          <div class="p-6">
            <h2 class="text-lg font-medium text-[color:var(--color-gray-900)] mb-4">Financial Performance</h2>
            
            <!-- Financial Metrics -->
            <div class="grid grid-cols-1 md:grid-cols-3 gap-4 mb-6">
              <KeyMetric 
                label="Current Market Cap" 
                value={formatMarketCap(company.marketCap)}
                trend="up"
                change="3.2% (1 month)"
              />
              <KeyMetric 
                label="Revenue (Latest)" 
                value={company.financials && company.financials.length > 0 ? `$${company.financials[0].revenue.toFixed(1)}B` : 'N/A'}
                trend={company.financials && company.financials.length > 1 ? (company.financials[0].revenue > company.financials[1].revenue ? "up" : "down") : undefined}
                change={company.financials && company.financials.length > 1 ? `${Math.abs(((company.financials[0].revenue / company.financials[1].revenue) - 1) * 100).toFixed(1)}% (YoY)` : 'N/A'}
              />
              <KeyMetric 
                label="R&D Spending (Latest)" 
                value={company.financials && company.financials.length > 0 ? `$${company.financials[0].rAndDSpending.toFixed(1)}B` : 'N/A'}
                trend={company.financials && company.financials.length > 1 ? (company.financials[0].rAndDSpending > company.financials[1].rAndDSpending ? "up" : "down") : undefined}
                change={company.financials && company.financials.length > 1 ? `${Math.abs(((company.financials[0].rAndDSpending / company.financials[1].rAndDSpending) - 1) * 100).toFixed(1)}% (YoY)` : 'N/A'}
              />
            </div>
            
            <!-- Financial Charts -->
            <div class="mb-6">
              <FinancialChart
                id="company-financials-chart"
                title="Key Financial Metrics"
                data={financialData}
              />
            </div>
            
            <!-- Financial Table -->
            <div class="overflow-x-auto">
              <table class="min-w-full divide-y divide-[color:var(--color-gray-200)]">
                <thead class="bg-[color:var(--color-gray-50)]">
                  <tr>
                    <th scope="col" class="py-3.5 pl-4 pr-3 text-left text-sm font-semibold text-[color:var(--color-gray-900)]">Year</th>
                    <th scope="col" class="px-3 py-3.5 text-left text-sm font-semibold text-[color:var(--color-gray-900)]">Revenue</th>
                    <th scope="col" class="px-3 py-3.5 text-left text-sm font-semibold text-[color:var(--color-gray-900)]">R&D Spending</th>
                    <th scope="col" class="px-3 py-3.5 text-left text-sm font-semibold text-[color:var(--color-gray-900)]">Net Income</th>
                    <th scope="col" class="px-3 py-3.5 text-left text-sm font-semibold text-[color:var(--color-gray-900)]">R&D as % of Revenue</th>
                  </tr>
                </thead>
                <tbody class="divide-y divide-[color:var(--color-gray-200)] bg-[color:var(--color-white)]">
                  {(company.financials && company.financials.length > 0) ? (
                    company.financials.map(finance => (
                      <tr>
                        <td class="whitespace-nowrap py-4 pl-4 pr-3 text-sm font-medium text-[color:var(--color-gray-900)]">{finance.year}</td>
                        <td class="px-3 py-4 text-sm text-[color:var(--color-gray-500)]">${finance.revenue.toFixed(1)}B</td>
                        <td class="px-3 py-4 text-sm text-[color:var(--color-gray-500)]">${finance.rAndDSpending.toFixed(1)}B</td>
                        <td class="px-3 py-4 text-sm text-[color:var(--color-gray-500)]">${finance.netIncome.toFixed(1)}B</td>
                        <td class="px-3 py-4 text-sm text-[color:var(--color-gray-500)]">
                          {((finance.rAndDSpending / finance.revenue) * 100).toFixed(1)}%
                        </td>
                      </tr>
                    ))
                  ) : (
                    <tr>
                      <td colspan="5" class="py-4 pl-4 pr-3 text-sm font-medium text-[color:var(--color-gray-500)] text-center">No financial data available</td>
                    </tr>
                  )}
                </tbody>
              </table>
            </div>
          </div>
        </Card>
      </div>
      
      <!-- News Tab (Placeholder) -->
      <div id="tab-content-news" role="tabpanel" aria-labelledby="tab-news" class="hidden">
        <Card>
          <div class="p-6">
            <h2 class="text-lg font-medium text-gray-900 mb-4">Latest News</h2>
            <p class="text-gray-600">News and press releases will be available soon.</p>
          </div>
        </Card>
      </div>
      
      <!-- Stock Tab -->
      <div id="tab-content-stock" role="tabpanel" aria-labelledby="tab-stock" class="hidden">
        <div class="grid grid-cols-1 lg:grid-cols-3 gap-6">
          <!-- Main Column -->
          <div class="lg:col-span-2 space-y-6">
            <!-- Stock Chart -->
            <Card>
              <div class="p-6">
                <div class="flex justify-between items-center mb-4">
                  <h2 class="text-lg font-medium text-[color:var(--color-gray-900)]">
                    Stock Performance
                    {company.stockSymbol && (
                      <span class="text-sm font-normal ml-2 text-[color:var(--color-gray-500)]">
                        {company.stockSymbol} • {company.stockExchange}
                      </span>
                    )}
                  </h2>
                  
                  <div class="flex space-x-2">
                    <button class="px-3 py-1 text-xs font-medium rounded-md bg-[color:var(--color-gray-100)] text-[color:var(--color-gray-700)] hover:bg-[color:var(--color-gray-200)]">1D</button>
                    <button class="px-3 py-1 text-xs font-medium rounded-md bg-[color:var(--color-primary-100)] text-[color:var(--color-primary-700)] hover:bg-[color:var(--color-primary-200)]">1W</button>
                    <button class="px-3 py-1 text-xs font-medium rounded-md bg-[color:var(--color-gray-100)] text-[color:var(--color-gray-700)] hover:bg-[color:var(--color-gray-200)]">1M</button>
                    <button class="px-3 py-1 text-xs font-medium rounded-md bg-[color:var(--color-gray-100)] text-[color:var(--color-gray-700)] hover:bg-[color:var(--color-gray-200)]">3M</button>
                    <button class="px-3 py-1 text-xs font-medium rounded-md bg-[color:var(--color-gray-100)] text-[color:var(--color-gray-700)] hover:bg-[color:var(--color-gray-200)]">1Y</button>
                    <button class="px-3 py-1 text-xs font-medium rounded-md bg-[color:var(--color-gray-100)] text-[color:var(--color-gray-700)] hover:bg-[color:var(--color-gray-200)]">5Y</button>
                  </div>
                </div>
                
                <!-- Stock price chart (placeholder) -->
                <div class="h-80 bg-[color:var(--color-gray-50)] rounded-lg border border-[color:var(--color-gray-200)] p-4 flex items-center justify-center">
                  <p class="text-[color:var(--color-gray-500)]">Stock price chart will be implemented with real-time data.</p>
                </div>
                
                <!-- Quick stats -->
                <div class="grid grid-cols-3 sm:grid-cols-6 gap-4 mt-4">
                  <div class="flex flex-col">
                    <span class="text-xs text-[color:var(--color-gray-500)]">Open</span>
                    <span class="font-medium">$182.34</span>
                  </div>
                  <div class="flex flex-col">
                    <span class="text-xs text-[color:var(--color-gray-500)]">High</span>
                    <span class="font-medium">$186.21</span>
                  </div>
                  <div class="flex flex-col">
                    <span class="text-xs text-[color:var(--color-gray-500)]">Low</span>
                    <span class="font-medium">$180.88</span>
                  </div>
                  <div class="flex flex-col">
                    <span class="text-xs text-[color:var(--color-gray-500)]">Volume</span>
                    <span class="font-medium">2.4M</span>
                  </div>
                  <div class="flex flex-col">
                    <span class="text-xs text-[color:var(--color-gray-500)]">P/E Ratio</span>
                    <span class="font-medium">18.2</span>
                  </div>
                  <div class="flex flex-col">
                    <span class="text-xs text-[color:var(--color-gray-500)]">Dividend</span>
                    <span class="font-medium">1.2%</span>
                  </div>
                </div>
              </div>
            </Card>
            
            <!-- Insider Trading Activity -->
            <Card>
              <div class="p-6">
                <h2 class="text-lg font-medium text-[color:var(--color-gray-900)] mb-4">Insider Trading Activity</h2>
                <table class="min-w-full divide-y divide-[color:var(--color-gray-200)]">
                  <thead class="bg-[color:var(--color-gray-50)]">
                    <tr>
                      <th scope="col" class="py-3.5 pl-4 pr-3 text-left text-sm font-semibold text-[color:var(--color-gray-900)]">Insider</th>
                      <th scope="col" class="px-3 py-3.5 text-left text-sm font-semibold text-[color:var(--color-gray-900)]">Title</th>
                      <th scope="col" class="px-3 py-3.5 text-left text-sm font-semibold text-[color:var(--color-gray-900)]">Transaction</th>
                      <th scope="col" class="px-3 py-3.5 text-left text-sm font-semibold text-[color:var(--color-gray-900)]">Date</th>
                      <th scope="col" class="px-3 py-3.5 text-left text-sm font-semibold text-[color:var(--color-gray-900)]">Shares</th>
                      <th scope="col" class="px-3 py-3.5 text-left text-sm font-semibold text-[color:var(--color-gray-900)]">Value</th>
                    </tr>
                  </thead>
                  <tbody class="bg-[color:var(--color-white)] divide-y divide-[color:var(--color-gray-200)]">
                    <tr>
                      <td class="py-4 pl-4 pr-3 text-sm font-medium text-[color:var(--color-gray-900)]">Jane Smith</td>
                      <td class="px-3 py-4 text-sm text-[color:var(--color-gray-500)]">CFO</td>
                      <td class="px-3 py-4 text-sm text-[color:var(--color-green-600)]">Buy</td>
                      <td class="px-3 py-4 text-sm text-[color:var(--color-gray-500)]">Mar 15, 2023</td>
                      <td class="px-3 py-4 text-sm text-[color:var(--color-gray-500)]">5,000</td>
                      <td class="px-3 py-4 text-sm text-[color:var(--color-gray-500)]">$925,000</td>
                    </tr>
                    <tr>
                      <td class="py-4 pl-4 pr-3 text-sm font-medium text-[color:var(--color-gray-900)]">Robert Johnson</td>
                      <td class="px-3 py-4 text-sm text-[color:var(--color-gray-500)]">CEO</td>
                      <td class="px-3 py-4 text-sm text-[color:var(--color-red-600)]">Sell</td>
                      <td class="px-3 py-4 text-sm text-[color:var(--color-gray-500)]">Feb 28, 2023</td>
                      <td class="px-3 py-4 text-sm text-[color:var(--color-gray-500)]">10,000</td>
                      <td class="px-3 py-4 text-sm text-[color:var(--color-gray-500)]">$1,860,000</td>
                    </tr>
                    <tr>
                      <td class="py-4 pl-4 pr-3 text-sm font-medium text-[color:var(--color-gray-900)]">Michael Davis</td>
                      <td class="px-3 py-4 text-sm text-[color:var(--color-gray-500)]">Director</td>
                      <td class="px-3 py-4 text-sm text-[color:var(--color-green-600)]">Buy</td>
                      <td class="px-3 py-4 text-sm text-[color:var(--color-gray-500)]">Jan 14, 2023</td>
                      <td class="px-3 py-4 text-sm text-[color:var(--color-gray-500)]">2,500</td>
                      <td class="px-3 py-4 text-sm text-[color:var(--color-gray-500)]">$462,500</td>
                    </tr>
                  </tbody>
                </table>
              </div>
            </Card>
          </div>
          
          <!-- Sidebar -->
          <div class="space-y-6">
            <!-- Analyst Recommendations -->
            <Card>
              <div class="p-6">
                <h2 class="text-lg font-medium text-[color:var(--color-gray-900)] mb-4">Analyst Recommendations</h2>
                
                <!-- Overall rating -->
                <div class="flex items-center mb-4">
                  <div class="flex-1">
                    <div class="flex items-center">
                      <span class="text-2xl font-bold text-[color:var(--color-gray-900)]">Buy</span>
                      <span class="ml-2 px-2 py-1 text-xs font-medium rounded-full bg-[color:var(--color-green-100)] text-[color:var(--color-green-800)]">
                        Strong Consensus
                      </span>
                    </div>
                    <p class="text-sm text-[color:var(--color-gray-500)]">Based on 18 analyst ratings</p>
                  </div>
                  <div class="w-16 h-16 rounded-full bg-[color:var(--color-green-100)] flex items-center justify-center">
                    <span class="text-xl font-bold text-[color:var(--color-green-600)]">4.2</span>
                  </div>
                </div>
                
                <!-- Rating breakdown -->
                <div class="space-y-2 mb-4">
                  <div class="flex items-center">
                    <span class="w-20 text-sm font-medium text-[color:var(--color-gray-700)]">Buy</span>
                    <div class="flex-1 h-2 bg-[color:var(--color-gray-200)] rounded-full overflow-hidden">
                      <div class="h-full bg-[color:var(--color-green-500)]" style="width: 70%"></div>
                    </div>
                    <span class="w-8 text-right text-sm text-[color:var(--color-gray-500)]">12</span>
                  </div>
                  <div class="flex items-center">
                    <span class="w-20 text-sm font-medium text-[color:var(--color-gray-700)]">Hold</span>
                    <div class="flex-1 h-2 bg-[color:var(--color-gray-200)] rounded-full overflow-hidden">
                      <div class="h-full bg-[color:var(--color-yellow-500)]" style="width: 22%"></div>
                    </div>
                    <span class="w-8 text-right text-sm text-[color:var(--color-gray-500)]">4</span>
                  </div>
                  <div class="flex items-center">
                    <span class="w-20 text-sm font-medium text-[color:var(--color-gray-700)]">Sell</span>
                    <div class="flex-1 h-2 bg-[color:var(--color-gray-200)] rounded-full overflow-hidden">
                      <div class="h-full bg-[color:var(--color-red-500)]" style="width: 11%"></div>
                    </div>
                    <span class="w-8 text-right text-sm text-[color:var(--color-gray-500)]">2</span>
                  </div>
                </div>
                
                <!-- Price targets -->
                <div class="border-t border-[color:var(--color-gray-200)] pt-4">
                  <h3 class="text-sm font-medium text-[color:var(--color-gray-900)] mb-3">Price Targets</h3>
                  <div class="flex justify-between items-center">
                    <div class="flex flex-col">
                      <span class="text-xs text-[color:var(--color-gray-500)]">Low</span>
                      <span class="font-medium">$165</span>
                    </div>
                    <div class="flex flex-col text-center">
                      <span class="text-xs text-[color:var(--color-gray-500)]">Average</span>
                      <span class="font-medium">$218</span>
                    </div>
                    <div class="flex flex-col text-right">
                      <span class="text-xs text-[color:var(--color-gray-500)]">High</span>
                      <span class="font-medium">$250</span>
                    </div>
                  </div>
                  <div class="mt-2 h-2 bg-[color:var(--color-gray-200)] rounded-full overflow-hidden relative">
                    <div class="absolute h-full w-1 bg-[color:var(--color-blue-500)]" style="left: 45%"></div>
                    <div class="absolute h-4 w-4 rounded-full bg-[color:var(--color-white)] border-2 border-[color:var(--color-blue-500)] top-1/2 transform -translate-y-1/2" style="left: 45%"></div>
                    <div class="h-full bg-gradient-to-r from-[color:var(--color-red-400)] via-[color:var(--color-yellow-400)] to-[color:var(--color-green-400)]"></div>
                  </div>
                  <div class="mt-1 text-xs text-[color:var(--color-gray-500)] text-center">
                    Current price: $184.50 (15.2% below average target)
                  </div>
                </div>
              </div>
            </Card>
            
            <!-- Peer Comparison -->
            <Card>
              <div class="p-6">
                <h2 class="text-lg font-medium text-[color:var(--color-gray-900)] mb-4">Peer Comparison</h2>
                <table class="min-w-full">
                  <thead>
                    <tr>
                      <th class="pb-2 text-left text-xs font-medium text-[color:var(--color-gray-500)]">Company</th>
                      <th class="pb-2 text-right text-xs font-medium text-[color:var(--color-gray-500)]">P/E</th>
                      <th class="pb-2 text-right text-xs font-medium text-[color:var(--color-gray-500)]">ROE</th>
                      <th class="pb-2 text-right text-xs font-medium text-[color:var(--color-gray-500)]">YTD</th>
                    </tr>
                  </thead>
                  <tbody class="divide-y divide-[color:var(--color-gray-200)]">
                    {relatedCompanies.slice(0, 4).map(related => (
                      related.company && (
                        <tr>
                          <td class="py-2 text-sm font-medium text-[color:var(--color-gray-900)]">{related.company.name}</td>
                          <td class="py-2 text-right text-sm text-[color:var(--color-gray-500)]">16.4</td>
                          <td class="py-2 text-right text-sm text-[color:var(--color-gray-500)]">18.2%</td>
                          <td class="py-2 text-right text-sm text-[color:var(--color-green-600)]">+12.4%</td>
                        </tr>
                      )
                    ))}
                    <tr class="bg-[color:var(--color-gray-50)]">
                      <td class="py-2 text-sm font-medium text-[color:var(--color-gray-900)]">{company.name}</td>
                      <td class="py-2 text-right text-sm text-[color:var(--color-gray-900)]">18.2</td>
                      <td class="py-2 text-right text-sm text-[color:var(--color-gray-900)]">21.5%</td>
                      <td class="py-2 text-right text-sm text-[color:var(--color-green-700)]">+15.7%</td>
                    </tr>
                  </tbody>
                </table>
              </div>
            </Card>
          </div>
        </div>
      </div>
    </div>
  </div>
</DashboardLayout>

<script>
  // Handle tab switching
  document.addEventListener('DOMContentLoaded', () => {
    const tabs = document.querySelectorAll('[role="tab"]');
    const panels = document.querySelectorAll('[role="tabpanel"]');
    
    tabs.forEach(tab => {
      tab.addEventListener('click', () => {
        const targetId = tab.getAttribute('aria-controls');
        
        // Hide all panels
        panels.forEach(panel => {
          panel.classList.add('hidden');
        });
        
        // Show target panel
        const targetPanel = document.getElementById(targetId);
        if (targetPanel) {
          targetPanel.classList.remove('hidden');
        }
      });
    });
  });
</script> 